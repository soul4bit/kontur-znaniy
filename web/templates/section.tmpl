<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{.Title}} · {{.AppName}}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@500;600&family=Manrope:wght@500;600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/static/app.css" />
</head>
<body class="app-page">
  <main class="atlas-shell">
    <header class="atlas-topbar">
      <div class="atlas-brand">
        <div class="atlas-mark">KZ</div>
        <div>
          <p class="eyebrow">{{.AppName}}</p>
          <h1 class="atlas-heading">Контур знаний</h1>
        </div>
      </div>

      <div class="atlas-topbar-meta">
        <a class="button button-ghost" href="/app">На главную</a>
        <span class="atlas-user-chip">@{{.User.Name}}</span>
        <form method="post" action="/auth/logout">
          <button class="button button-ghost" type="submit">Выйти</button>
        </form>
      </div>
    </header>

    <section class="atlas-layout atlas-layout-two">
      <aside class="atlas-rail">
        <article class="atlas-card atlas-sections-sidebar">
          <p class="panel-label">Разделы</p>
          <ul class="atlas-nav">
            {{range .Sections}}
              <li class="atlas-nav-item">
                <a class="atlas-nav-link {{if eq $.CurrentSectionSlug .Slug}}atlas-nav-link-active{{end}}" href="/app/section?slug={{.Slug}}">
                  <span class="atlas-nav-title">{{.Name}}</span>
                </a>
                <ul class="atlas-nav-sub">
                  {{range .Subsections}}
                    <li>{{.}}</li>
                  {{end}}
                </ul>
              </li>
            {{end}}
          </ul>
          <a class="atlas-side-tool {{if eq .CurrentPage "s3"}}atlas-side-tool-active{{end}}" href="/app/s3">Проверка S3</a>
        </article>
      </aside>

      <section class="atlas-main">
        <article class="atlas-card atlas-main-head">
          <p class="panel-label">Раздел</p>
          <h2 class="atlas-section-title">{{.CurrentSection.Name}}</h2>
          <p class="atlas-hero-text">Собирайте рабочие заметки коротко и конкретно, чтобы возвращаться к ним без боли.</p>
          <div class="atlas-actions">
            <a class="button button-primary" href="/app/article/new?section={{.CurrentSection.Slug}}">Новая статья</a>
          </div>
        </article>

        {{if .Success}}
          <p class="alert alert-success">{{.Success}}</p>
        {{end}}

        {{if .SectionArticles}}
          <div class="atlas-article-list">
            {{range .SectionArticles}}
              <article class="atlas-card atlas-article-card">
                <div class="atlas-article-meta">
                  <span>Автор: {{.AuthorName}}</span>
                  <span>{{.UpdatedAt.Format "02.01.2006 15:04"}}</span>
                </div>
                <h3>{{.Title}}</h3>
                <p class="atlas-article-body">{{.Body}}</p>
              </article>
            {{end}}
          </div>
        {{else}}
          <article class="atlas-card">
            <p class="atlas-empty">В этом разделе пока нет статей. Нажмите «Новая статья» и добавьте первую.</p>
          </article>
        {{end}}
      </section>
    </section>
  </main>
</body>
</html>
