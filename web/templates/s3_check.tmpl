<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{.Title}} · {{.AppName}}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@500;600&family=Manrope:wght@500;600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/static/app.css" />
</head>
<body class="app-page">
  <main class="atlas-shell">
    <header class="atlas-topbar">
      <div class="atlas-brand">
        <div class="atlas-mark">KZ</div>
        <div>
          <p class="eyebrow">{{.AppName}}</p>
          <h1 class="atlas-heading">Контур знаний</h1>
        </div>
      </div>

      <div class="atlas-topbar-meta">
        <a class="button button-ghost" href="/app">На главную</a>
        <span class="atlas-user-chip">@{{.User.Name}}</span>
      </div>
    </header>

    <section class="atlas-layout atlas-layout-two">
      <aside class="atlas-rail">
        <article class="atlas-card atlas-sections-sidebar">
          <p class="panel-label">Разделы</p>
          <ul class="atlas-nav">
            {{range .Sections}}
              <li class="atlas-nav-item">
                <a class="atlas-nav-link {{if eq $.CurrentSectionSlug .Slug}}atlas-nav-link-active{{end}}" href="/app/section?slug={{.Slug}}">
                  <span class="atlas-nav-title">{{.Name}}</span>
                </a>
                <ul class="atlas-nav-sub">
                  {{range .Subsections}}
                    <li>{{.}}</li>
                  {{end}}
                </ul>
              </li>
            {{end}}
          </ul>
          <a class="atlas-side-tool {{if eq .CurrentPage "s3"}}atlas-side-tool-active{{end}}" href="/app/s3">Проверка S3</a>
        </article>
      </aside>

      <section class="atlas-main">
        <article class="atlas-card atlas-main-head">
          <p class="panel-label">Проверка S3</p>
          <h2 class="atlas-section-title">Бакет и ключи под рукой</h2>
          <p class="atlas-hero-text">Нажмите кнопку ниже, и сервер проверит доступность бакета по текущим переменным окружения.</p>
          <div class="atlas-actions">
            <a class="button button-primary" href="/app/s3?run=1">Запустить проверку</a>
          </div>
        </article>

        <article class="atlas-card">
          <ul class="atlas-list">
            <li><span>Endpoint</span><em>{{.S3Endpoint}}</em></li>
            <li><span>Bucket</span><em>{{.S3Bucket}}</em></li>
            <li><span>Публичный домен</span><em>{{if .S3PublicBaseURL}}{{.S3PublicBaseURL}}{{else}}не задан{{end}}</em></li>
          </ul>

          {{if .S3Check}}
            {{if .S3Check.OK}}
              <p class="alert alert-success">{{.S3Check.Message}}</p>
            {{else}}
              <p class="alert alert-error">{{.S3Check.Message}}</p>
            {{end}}
          {{else}}
            <p class="atlas-empty">Проверка еще не запускалась.</p>
          {{end}}

          {{if .S3PublicBaseURL}}
            <p class="atlas-empty" style="margin-top:10px;">
              Пример ссылки на файл: <code>{{.S3PublicBaseURL}}/articles/example.txt</code>
            </p>
          {{end}}
        </article>
      </section>
    </section>
  </main>
</body>
</html>
